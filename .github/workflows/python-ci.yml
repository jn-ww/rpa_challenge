name: CI (Python + Playwright)

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  lint:
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
          cache-dependency-path: requirements.txt
      - run: |
          python -m pip install -r requirements.txt
          python -m pip install --upgrade ruff black
      - name: Ruff (lint)
        run: ruff check . || echo "::warning ::ruff found issues (non-blocking)"
      - name: Black (format check)
        run: black --check . || echo "::warning ::black would reformat (non-blocking)"

  run:
    runs-on: ubuntu-latest
    # remove `needs: lint` so this always runs
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
          cache-dependency-path: requirements.txt
      - name: Install Playwright + deps
        run: |
          pip install -r requirements.txt
          python -m playwright install --with-deps
      - name: Run challenge (headless, perf) on sample data
        run: |
          mkdir -p screenshots
          python -m src.main --file data/sample.csv --headless --perf --log-level WARNING
          [ -f screenshots/run_summary.json ] && cat screenshots/run_summary.json || true
      - name: Upload screenshots
        uses: actions/upload-artifact@v4
        with:
          name: screenshots
          path: screenshots/*
          if-no-files-found: warn
